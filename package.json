{
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.17.3",
    "path": "^0.12.7"
  },
  "scripts": {
    "start":"node server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  }
}
